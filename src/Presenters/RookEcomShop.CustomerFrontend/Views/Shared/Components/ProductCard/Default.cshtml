@using RookEcomShop.ViewModels.Dto;
@model ProductDto;
@{
    var serverImage = new ServerImageVM { Url = Model.ImgUrls.FirstOrDefault(), Alt = Model.Name, ClassName = "h-full w-full object-cover object-center lg:h-full lg:w-full" };
}

<a asp-controller="Products" asp-action="Details" asp-route-id="@Model.Id" class="group relative">
    <div
        class="aspect-h-1 aspect-w-1 w-full overflow-hidden rounded-md bg-gray-200 lg:aspect-none group-hover:opacity-75 lg:h-80">
      
            <partial name="~/Views/Shared/Partials/_ServerImage.cshtml"
                     model="@serverImage" />
       
    </div>
    <div class="mt-4 grid gap-2">
        <h3 class="text-lg text-gray-700">
            <div>
                <span aria-hidden="true" class="absolute inset-0"></span>
                @Model.Name
            </div>
        </h3>
        <p class="text-sm text-red-700">From $@Model.Price</p>
    </div>
</a>